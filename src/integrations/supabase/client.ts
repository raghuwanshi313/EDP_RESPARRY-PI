// Offline mode - Supabase client disabled
// This app runs completely offline using IndexedDB for storage

export const supabase = null;

// Note: This project uses local storage via IndexedDB
// See src/services/storageService.js for all storage operations